<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.unimib.ginlemons" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_personal"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_personal"
        android:supportsRtl="true"
        android:theme="@style/Theme.GinLemons" >
        <activity
            android:name=".ui.RicetteInfoActivity"
            android:exported="false"
            android:label="@string/title_activity_ricette_info"
            android:theme="@style/Theme.GinLemons.NoActionBar" />
        <activity
            android:name=".RicetteInfoActivity"
            android:exported="false" />
        <activity
            android:name=".ui.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>